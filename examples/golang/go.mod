module example.com/imageconv

go 1.24.7

// For local testing - use the local repository
// Uncomment the line below when testing locally in the repository
// replace github.com/ideamans/libnextimage/golang => ../../golang

require github.com/ideamans/libnextimage/golang v0.3.0
